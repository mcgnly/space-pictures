import imageReducer from "./imageReducer";
import { combineReducers } from "redux";

export default combineReducers({
	//here you build the single-book-of-truth from the individual chapters
	imageReducer
});
